# Firebase Configuration
# Copy this file to .env and fill in your values
# These are safe to expose in client-side code, but using env vars allows:
# 1. Different configs for dev/staging/prod
# 2. Prevents accidental exposure in GitHub searches
# 3. Follows security best practices

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Admin Configuration  
# Comma-separated list of admin emails (for initial setup only)
# Production should use Firebase Custom Claims instead
VITE_ADMIN_EMAILS=admin@example.com
